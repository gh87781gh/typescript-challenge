import { describe, it, expect } from 'vitest';
import { findMaxNumber } from './maxNumber';

// [1, 3, 2] -> 3
// [-1, -3, -2] -> -1
// [5]-> 5

describe('找出最大數', () => {
  it('應該能正確找出陣列中的最大數', () => {
  });
});