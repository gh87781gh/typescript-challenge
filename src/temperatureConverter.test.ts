import { describe, it, expect } from 'vitest';
import { celsiusToFahrenheit } from './temperatureConverter';

// 0 -> 32
// 100 -> 212

describe('溫度轉換器', () => {
  it('正確將攝氏溫度轉換為華氏溫度', () => {
  });
});
